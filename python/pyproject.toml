[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "molrs"
version = "0.1.0"
description = "Python bindings for molcore"
authors = [{ name = "MolCrafts" }]
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]

