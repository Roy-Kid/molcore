// Integration tests for IO modules
// These tests are only enabled with the `slow-tests` feature flag
// Test data will be automatically downloaded if not present

#[cfg(feature = "slow-tests")]
pub mod test_xyz;
